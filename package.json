{
  "name": "whatsapp-reader",
  "version": "0.3.0",
  "description": "Read-only, offline CLI for the WhatsApp for Mac local database.",
  "type": "module",
  "private": true,
  "engines": { "node": ">=24" },
  "bin": { "wa": "./src/cli.ts" },
  "scripts": {
    "test": "node --no-warnings --test \"test/**/*.test.ts\"",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.7.0",
    "@types/node": "^24.0.0"
  }
}
